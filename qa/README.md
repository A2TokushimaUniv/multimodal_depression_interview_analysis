# アンケートデータの変換スクリプト

アンケートデータから、各項目を数値化するスクリプト

## 実行手順

1. アンケートの生データ（Excelファイル）を開き「名前をつけて保存」からCSV形式に変換した後、`data/qa`以下に格納する
   - なお、理工学部のアンケートは`riko_before_raw.csv`, `riko_after_raw.csv`という名前で保存し、医学部のアンケートは`igaku_before_raw.csv`, `igaku_after_raw.csv`（成人の事前事後アンケート）, `igaku_child_before_raw.csv`（児童思春期の事前アンケート）, `igaku_parent_before_raw.csv`（保護者の事前アンケート）という名前で保存する
2. `python main.py`を実行する

## 実行結果

医学部と理工学部の被験者のアンケートの各項目を数値化し連結したデータが生成される

## 処理の流れ

1. 理工学のアンケートを整形する（一部テスト時のデータが入っていたりなどで汚いので）
2. アンケート項目をまとめて数値に変換する（BIG5 を 5 つの性格ごとに点数化したりなど）
3. 医学・理工のアンケートをまとめて一つの CSV にする

## その他のスクリプト

- `interview_label.py`: 上記を実行して得られたCSVをもとに面談のラベルを作成するスクリプト
- `phq_stats.py`: 上記を実行して得られたCSVをもとにPHQ-9 スコアの分布図を作成するスクリプト
- `age_stats.py`: 上記を実行して得られたCSVをもとに年齢の分布図を作成するスクリプト